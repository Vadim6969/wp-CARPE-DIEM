# CARPE DIEM 2026 — интернет-магазин на WordPress + WooCommerce

План реализации. Стиль: тёмный готик-стритвир по референсам (главная, корзина, карточка товара).

---

## 0. Решения (и что сознательно НЕ делаем)

| Вопрос | Решение | Почему не иначе |
|---|---|---|
| Локальная среда | `npx @wordpress/env` (Docker уже стоит, Node 22) | Не ставим MAMP/Local — один `.wp-env.json` и `npx wp-env start` |
| Тема | Своя минимальная классическая тема `carpediem` (~8 файлов) | Дизайн кастомный на 100%. Дочка Storefront = борьба с чужим CSS через `!important`; блочная тема = верстать арт-дирекшн мышкой в редакторе |
| Сборка фронта | Нет сборки. Чистый CSS + чистый JS, подключение через `wp_enqueue_*` | Vite/webpack для 3 страниц — лишний слой. Добавим, если CSS перевалит за ~2000 строк |
| Корзина и чекаут | Классические шорткоды `[woocommerce_cart]` / `[woocommerce_checkout]` вместо блочных | Блочный чекаут перестраивается только через сборку JS-плагина; под такой макет дешевле PHP-шаблоны |
| Конструктор страниц | Нет (ни Elementor, ни ACF Pro) | Главная — один PHP-шаблон `front-page.php`. Правки контента — через `theme.json`-независимые опции/Кастомайзер по необходимости |
| «Фишки» товара, состав, плотность, уход | Атрибуты товара WooCommerce | Ноль плагинов, ноль мета-боксов, редактируется в админке из коробки |
| Таблица размеров | Массив по слагу категории в `inc/woo.php` (`carpediem_size_table`) | Три почти одинаковых партиала не нужны; плагины таблиц тянут свой UI |
| Цветовые свотчи | ~60 строк в `main.js`: кнопки поверх нативных `select` Woo | Плагин дал бы обобщённый UI, который всё равно пришлось бы перекрашивать под макет — вышло дешевле написать |
| Оплата | ЮKassa (офиц. плагин): Visa/MC/МИР/SBP/ApplePay | Совпадает с иконками на макете корзины |
| Доставка | Старт: flat rate + «бесплатно от 15 000 ₽». Позже — офиц. плагин СДЭК | Интеграция ПВЗ — отдельная фаза, не блокирует запуск |
| SEO/микроразметка | Свой `inc/seo.php` (~100 строк): description, Open Graph, фавикон, noindex служебных страниц | Woo уже отдаёт Product/Offer/BreadcrumbList, ядро WP — canonical и `wp-sitemap.xml`. Плагин нужен только если заказчик захочет править title/description руками |
| Кэш | Кэш хостинга/nginx | Плагины кэша на 3 страницах — оверкилл |

**Итого плагинов: 2** (WooCommerce + ЮKassa на запуске). Свотчи, SEO-мета и таблицы размеров написали сами. Плюс Safe SVG, если логотип отдаём в SVG.

---

## 1. Дизайн-система

Забирается из макетов, слегка «разбавлено»: чуть больше воздуха, тонкие вертикальные акценты-кресты как разделители секций, hover — свечение молнии (subtle glow), а не смещение.

### Цвета (CSS-переменные в `:root`)

```
--bg:        #050505   /* фон страницы */
--bg-panel:  #0b0b0c   /* карточки, панели, футер */
--bg-elev:   #121214   /* инпуты, hover-панели */
--line:      #1e1e21   /* границы 1px */
--line-soft: #141416
--text:      #e8e6e3   /* основной */
--text-dim:  #adadb4   /* подписи, мета — 8.4:1 */
--text-mute: #82828b   /* дисклеймеры — 4.9:1, ниже не опускаемся */
--accent:    #d9d4cc   /* «серебро»: заголовки, иконки */
--accent-hi: #ffffff   /* hover / активное */
--danger:    #b3453c   /* ошибки, удаление */
```

Никаких ярких брендовых цветов — контраст строится на свете/тени.

**Светлая тема** — те же токены с «бумажной» палитрой в `:root[data-theme="light"]`:
`--bg: #f7f5f2` · `--bg-panel: #ffffff` · `--line: #ddd7cf` · `--text: #16161a` · `--text-dim: #4e4e57` · `--accent: #23232a`.
Переключается кнопкой в шапке, выбор хранится в `localStorage`, тёмная — по умолчанию.
Инлайн-скрипт в `<head>` ставит тему до первой отрисовки, чтобы не мелькал чужой фон.
Всё, что зависит от темы (свечения, градиенты hero, цвет молний и монограммы, орнаменты), тоже вынесено в токены —
захардкоженных цветов в правилах нет.

### Типографика

- Заголовки: **Cormorant Garamond** (есть кириллица), 300/400, `letter-spacing: .08em`, `text-transform: uppercase`.
- Интерфейс/текст: **Manrope** (кириллица), 400/500, базовый кегль 16px, интерлиньяж 1.75.
- Трекинг мелких подписей — не больше `.2em`: у референса он до `.5em`, читается плохо.
- Подключение: локальные `.woff2` в теме (не Google CDN) — быстрее и без внешних запросов.
- Шкала: 12 / 13 / 15 / 18 / 24 / 34 / 48 / 64 px, межстрочный 1.5 для текста, 1.15 для заголовков.

### Сетка и ритм

- Контейнер `max-width: 1440px`, боковые поля 24px (моб.) / 48px (десктоп).
- Модуль отступов: 8px. Секции — 96–120px по вертикали на десктопе, 56–64px на мобиле.
- Границы всегда 1px `--line`, радиусов нет (углы острые — это часть стиля).

### Компоненты (переиспользуемые классы)

`.btn` (прямоугольная, 1px рамка, hover — заливка `--bg-elev` + белый текст), `.btn--ghost`, `.section-title` (с крестами-разделителями `✚` по бокам через `::before/::after`), `.card` (панель с 1px рамкой), `.qty` (− 1 +), `.usp-bar` (4 иконки-преимущества), `.divider-cross`.

---

## 2. Структура темы

```
wp-content/themes/carpediem/
├── style.css              # шапка темы (метаданные) + импорт не нужен
├── functions.php          # enqueue, supports, woo-хуки, меню, размеры картинок
├── header.php             # верхняя панель: меню / логотип / поиск-аккаунт-корзина
├── footer.php             # 4 колонки + копирайт + USP-бар
├── front-page.php         # ГЛАВНАЯ (все секции)
├── page.php               # обычные страницы (О нас, Доставка, FAQ…)
├── index.php              # фолбэк (обязателен WP)
├── 404.php
├── assets/
│   ├── css/main.css       # весь дизайн (один файл, секции комментариями)
│   ├── js/main.js         # меню-моб, мини-корзина, галерея товара, аккордеоны
│   ├── fonts/*.woff2
│   └── img/               # декор: молнии, текстура, кресты (SVG)
├── inc/
│   ├── woo.php            # все правки WooCommerce (хуки, а не копипаста шаблонов)
│   ├── one-click.php      # «Купить в 1 клик» (фаза 4)
│   └── size-tables/       # hoodie.php, tshirt.php, bag.php…
└── woocommerce/           # ТОЛЬКО реально переопределённые шаблоны
    ├── single-product.php
    ├── content-single-product.php
    ├── content-product.php          # карточка в сетке каталога
    ├── cart/cart.php
    ├── cart/cart-totals.php
    └── global/quantity-input.php
```

**Правило:** сначала пытаемся сделать хуком в `inc/woo.php`, копируем шаблон в `woocommerce/` только когда хуками не выходит. Каждый скопированный шаблон — это долг на апдейтах Woo.

---

## 3. Контент-модель

### Категории товаров
`Худи`, `Футболки`, `Свитшоты`, `Сумки`, `Аксессуары` (кепки, подвески, цепи).
Каждой категории — картинка-обложка (используется в сетке на главной) и `slug` латиницей: `hoodie`, `tshirt`, `sweatshirt`, `bags`, `accessories`.

### Атрибуты (глобальные, `Товары → Атрибуты`)
| Атрибут | Тип | Для вариаций | Пример |
|---|---|---|---|
| `Размер` (pa_size) | select | да | XS…XXL, ONE SIZE |
| `Цвет` (pa_color) | select + swatch | да | Чёрный, Чёрный/Коричневый |
| `Особенности` (pa_features) | select, множ. | нет | «Термохромная ткань Premium», «Вышивка логотипа», … → рендерим списком с ✚ |
| `Состав` (pa_material) | select | нет | 100% Cotton Premium |
| `Плотность` (pa_density) | select | нет | 380 GSM |
| `Уход` (pa_care) | select, множ. | нет | «Стирка до 30°C», «Не отбеливать», … → рендерим иконками |

Уход: маппинг `значение атрибута → SVG-иконка` — один ассоциативный массив в `inc/woo.php`. Нет иконки — выводим текстом.

### Товары
Вариативные (размер × цвет). Обязательно: 4–6 фото, галерея, описание, вес/габариты (для расчёта доставки позже), «Новинка» — через тег `новинка` (бейдж на карточке), перекрёстные продажи (cross-sells) — для блока «С этим товаром покупают», а на странице корзины Woo сам выведет cross-sells в «Рекомендуем».

### Служебные страницы
Главная, Каталог (shop), Корзина, Оформление заказа, Мой аккаунт, О нас, Доставка и оплата, Возврат, Контакты, FAQ, Таблица размеров, Уход за вещами, Сотрудничество, Политика конфиденциальности, Оферта, Согласие на обработку персональных данных (152-ФЗ — обязательный чекбокс на чекауте).

---

## 4. Страницы — что именно верстаем

### 4.1 Главная (`front-page.php`)

1. **Хедер** (`header.php`, sticky, полупрозрачный blur при скролле): слева меню `COLLECTIONS / CATALOG / ABOUT / LOOKBOOK`, по центру логотип «CARPE DIEM 2026», справа `SEARCH / ACCOUNT / CART (n)`. Мобильный — бургер + off-canvas.
2. **Hero**: фон-молнии (WebP, `object-fit: cover`, ~1900×1200, ≤300 КБ), крупная монограмма-логотип по центру, заголовок, год, крест. Плавное появление (`opacity/translateY`, `prefers-reduced-motion` уважаем).
3. **Блок «СПАСИБО ЗА ТО, ЧТО ТЫ С НАМИ»**: заголовок + англ. подзаголовок + 3 строки текста + кнопка «ЧИТАТЬ ДАЛЬШЕ», по краям метки `+ UNITY` / `FREEDOM +`.
4. **Сетка категорий** (5 плиток, заголовок «КАТЕГОРИИ» с крестами): картинка + название + «ПЕРЕЙТИ ✚». Данные — из `product_cat`, а не хардкод. На мобиле — горизонтальный скролл-снап.
5. **Блок «CARPE DIEM — STYLE OF SOUL»**: слева изображение, справа текст + кнопка «УЗНАТЬ НАШУ ИСТОРИЮ».
6. **USP-бар** (4 колонки с иконками): Стиль и дух / Качество превыше всего / Для тех, кто понимает / Быстрая доставка. Партиал, переиспользуется на карточке товара и в корзине.
7. **Футер**: логотип-колонка, «ИНФОРМАЦИЯ», «ПОДДЕРЖКА», «СОЦИАЛЬНЫЕ СЕТИ» (иконки), копирайт. Меню — через `register_nav_menus`, не хардкод.

*Разбавляем от макета:* добавляем между (4) и (5) узкую строку-«бегущую строку» с брендовым слоганом (CSS-анимация, без JS-библиотек) — оживляет длинную тёмную страницу.

### 4.2 Карточка товара (`woocommerce/single-product.php`)

- Хлебные крошки + декоративная монограмма и молнии в шапке страницы.
- **Галерея:** вертикальные превью слева, крупное фото справа. Берём стандартную Woo-галерею (flexslider уже в комплекте), перекрашиваем CSS + переносим thumbnails влево. Не тащим свой слайдер.
- **Правая колонка:** бейдж «НОВИНКА», H1, цена, свотчи цвета, кнопки размеров, `ДОБАВИТЬ В КОРЗИНУ ✚`, `КУПИТЬ В 1 КЛИК`, список особенностей (атрибут `pa_features`), строка «В избранное / Поделиться».
- Подпись под фото («Термохромный эффект: меняет цвет при тепле») — короткое поле, берём из атрибута или excerpt товара.
- **USP-бар** (тот же партиал).
- **Три колонки:** «ОПИСАНИЕ» (описание товара) / «МАТЕРИАЛЫ И УХОД» (атрибуты + иконки ухода) / «ТАБЛИЦА РАЗМЕРОВ» (партиал по категории). На мобиле — аккордеоны.
- **«С ЭТИМ ТОВАРОМ ПОКУПАЮТ»** — cross-sells, карусель на CSS scroll-snap + две стрелки (10 строк JS).
- Микроразметка Product/Offer — от Rank Math, проверяем валидатором.

**«Купить в 1 клик»:** модалка «Имя + телефон + согласие», `admin-ajax` → `wc_create_order()` со статусом `pending` + пометка в заказе. ~50 строк в `inc/one-click.php`. Обязательно: nonce, честная валидация телефона, rate-limit по IP (transient), чекбокс согласия 152-ФЗ.

### 4.3 Корзина (`woocommerce/cart/cart.php`)

- Шапка страницы с заголовком «КОРЗИНА» и крошками.
- Таблица: фото / название + цвет + размер / цена / кол-во (− 1 +) / сумма / ✕. На мобиле — карточки, не таблица.
- Правая панель «ВАШ ЗАКАЗ»: товары (n) / доставка «Рассчитывается на следующем шаге» / промокод / **ИТОГО** / `ОФОРМИТЬ ЗАКАЗ ✚` / `ПРОДОЛЖИТЬ ПОКУПКИ` / иконки платёжных систем.
- Блок «СПАСИБО ЗА ПОДДЕРЖКУ» под таблицей (декоративный).
- «РЕКОМЕНДУЕМ» — cross-sells (стандартный хук Woo).
- Обновление количества — AJAX (Woo умеет из коробки при `update_cart` через JS-фрагменты; вешаемся на `wc_fragments`, свой велосипед не пишем).
- Пустая корзина — отдельное состояние с ссылкой в каталог. Не забыть.

### 4.4 Каталог, чекаут, аккаунт

- **Каталог:** сетка карточек в стиле блока «Рекомендуем», фильтры по цене/размеру/цвету — стандартные виджеты Woo, стилизованные. Пагинация вместо бесконечного скролла.
- **Чекаут:** стандартный Woo-чекаут, только CSS + обязательный чекбокс согласия на обработку ПДн. Не переписываем.
- **Аккаунт:** только CSS, без правок логики.

---

## 5. Фазы работ

### Фаза 1 — фундамент (полдня) ✅ сделано
- [x] `.wp-env.json`, `npx wp-env start`, WP на `localhost:8888`, локаль `ru_RU`
- [x] Установка WooCommerce, мастер настройки (валюта RUB, страна RU, единицы)
- [x] Каркас темы `carpediem` (style.css, functions.php, index/header/footer/page/404), активация
- [x] `add_theme_support('woocommerce')` + галерея (zoom, lightbox, slider), размеры картинок
- [x] `git init`, `.gitignore` (в репозиторий — только `wp-content/themes/carpediem`, `.wp-env.json`, `PLAN.md`)

**Проверка фазы:** сайт открывается, страница товара из демо-данных Woo рендерится в теме без фаталов.

### Фаза 2 — дизайн-система и главная (1–1.5 дня) ✅ сделано (кроме фотоассетов)
- [x] `main.css`: переменные, шрифты, типографика, `.btn`, `.card`, `.section-title`, `.usp-bar`
- [x] `header.php` + `footer.php` + мобильное меню
- [x] `front-page.php`: все 7 секций
- [ ] Ассеты: hero-молнии, текстуры, SVG-кресты и иконки (WebP + `loading="lazy"` для всего ниже первого экрана)

**Проверка:** главная 1:1 к макету на 1440px, живая на 375px, Lighthouse Performance ≥ 85 на десктопе.

### Фаза 3 — каталог и карточка товара (1.5–2 дня) ✅ сделано на «рыбе» (ждём фото)
- [x] Атрибуты, категории, 8–10 реальных товаров с фото и вариациями
- [x] ~~Плагин свотчей~~ — свои свотчи, ~60 строк JS вместо плагина
- [x] `content-product.php` (карточка в сетке), архив категории, фильтры
- [x] `single-product.php`: галерея слева-превью, правая колонка, три информационные колонки, таблица размеров, cross-sells
- [x] Иконки ухода (маппинг атрибут → SVG)

**Проверка:** товар с двумя цветами и шестью размерами добавляется в корзину, вариация меняет фото и цену.

### Фаза 4 — корзина, чекаут, заказ (1–1.5 дня) ✅ сделано
- [x] `cart.php` + `cart-totals.php` по макету, мобильные карточки, пустое состояние
- [x] Промокоды (Woo-купоны), проверка на живом купоне
- [x] Стилизация чекаута, чекбокс согласия 152-ФЗ, поля под РФ (телефон обязателен)
- [x] Доставка: flat rate + бесплатно от 15 000 ₽, зоны Россия / Мир
- [x] «Купить в 1 клик» (nonce + валидация + rate-limit)
- [x] Письма заказа: логотип, тёмная шапка, русские тексты

**Проверка:** сквозной тестовый заказ — корзина → чекаут → заказ в админке → письмо покупателю и админу.

### Фаза 5 — оплата, контент, запуск (1 день) — сделано всё, что не требует реквизитов
- [ ] ЮKassa: тестовый магазин → успешный тестовый платёж → боевые ключи *(ждём реквизиты)*
- [x] Служебные страницы (оферта, политика, доставка, FAQ, возврат)
- [x] ~~Rank Math~~ — обошлись своим `inc/seo.php`: description + Open Graph + фавикон; микроразметку Product/Offer/Breadcrumb даёт сам Woo, sitemap и канониклы — ядро WP
- [x] Favicon, OG-картинки, `robots.txt`
- [ ] Метрика/аналитика (Яндекс.Метрика + Woo-события: add_to_cart, purchase) *(ждём номер счётчика)*
- [ ] Перенос на хостинг, HTTPS, бэкапы, письмо с домена (SMTP), 301-редиректы если есть старый сайт *(ждём хостинг)*

**Проверка запуска:** реальный платёж на 10 ₽ и возврат; письма не в спаме; проверка на мобиле с реального телефона.

**Итого: ~5–6 рабочих дней** без учёта подготовки фото и текстов.

---

## 6. Что после запуска (не делаем сейчас)

Интеграция СДЭК/Boxberry с картой ПВЗ · избранное (сейчас — иконка-заглушка или localStorage) · программа лояльности · lookbook как отдельный тип записи · подписка на дропы · многоязычность · отзывы с фото. Добавляем, когда попросят — не раньше.

---

## 7. Риски и как их снимаем

| Риск | Действие |
|---|---|
| Тяжёлые тёмные фото → медленная загрузка | Только WebP, hero ≤ 300 КБ, `srcset`, `fetchpriority=high` для hero и `loading=lazy` для остального |
| Контраст текста на тёмном фоне (доступность) | Проверить каждый оттенок серого; фокус-обводка видимая; `prefers-reduced-motion` |
| Обновление WooCommerce ломает скопированные шаблоны | Держим минимум переопределений, версии шаблонов фиксируем в комментарии, проверяем `Статус → Шаблоны` |
| ЮKassa требует юрлицо/ИП и договор | Начать оформление на Фазе 1, не на Фазе 5 |
| Кириллица в шрифтах | Cormorant Garamond и Manrope — оба с кириллицей, subset ru+en |

---

## 8. Что нужно от заказчика до старта Фазы 3

1. Фото товаров (4–6 на позицию, единый ракурс и свет).
2. Тексты: описания товаров, «О нас», доставка, возврат, оферта, политика.
3. Логотип-монограмма в SVG + фавикон.
4. Реквизиты для ЮKassa и ссылки на соцсети.
5. Реальные размерные сетки по категориям.
